import { LatinLettersAndNumbersValidatorDirective } from './latin-letters-and-numbers-validator.directive';

describe('LatinLettersAndNumbersValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LatinLettersAndNumbersValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
