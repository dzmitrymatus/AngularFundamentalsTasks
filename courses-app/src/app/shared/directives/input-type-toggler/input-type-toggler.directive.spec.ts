import { InputTypeTogglerDirective } from './input-type-toggler.directive';

describe('InputTypeTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTypeTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
